{
  "description": "Lifecycle logging for the agent workflow: START/STOP lines per subagent into the project's logs/agent-workflow.log, plus machine events in logs/pipeline-events.jsonl for the pipeline dashboard (which opens itself as a herdr pane when Claude runs inside herdr). The version-check hook is intentionally absent — plugin installs update through the plugin system.",
  "hooks": {
    "SubagentStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/.claude/hooks/log-agent-lifecycle.sh START"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/.claude/hooks/log-agent-lifecycle.sh STOP"
          }
        ]
      }
    ]
  }
}
